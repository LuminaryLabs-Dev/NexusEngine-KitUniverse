# Agent Memory

Status: active

## Purpose

Durable agent-facing memory for NexusEngine-KitUniverse.

## Lasting Notes

- Use `.agent/` as the repo-local coordination workspace.
- Prefer one simple entry point for orchestration, with sub-harnesses doing scoped work.
- Verify local-model endpoint, model listing, and chat usability separately.
- As of 2026-07-08, `http://10.0.0.38:1234` listed `lfm2.5-350m-heretic-high-reasoning` as loaded.
- A 2026-07-08 strict-output chat probe returned the required token plus extra prose, so sub-harnesses need schema/output gates.
- Chains should call `ask_provider`; provider adapters own LM Studio details.
- The first chain harness is `chain-ask-for-domain-list`, with three prompt steps and artifacts under `runs/`.
- Lesson: `lfm2.5-350m-heretic-high-reasoning` is reachable but did not satisfy strict final-list quality gates; `lfm2.5-8b-a1b` passed the same chain on 2026-07-08.
- Workflow harnesses live in separate folders under `workflow_harnesses/`; Chainstormer defaults to `10.0.0.137` and `lfm2.5-350m-heretic-high-reasoning-i1`.
- Chainstormer 2-loop smoke passed on 2026-07-08 and wrote a JSON list at `runs/workflow-harnesses/chainstormer/20260708-050412/chainstorm.json`.
- Bucket service convention: buckets are folders under `buckets/`; intake writes raw records, heuristic-state blocks duplicate final submissions, llm-step is present but auto-passes until implemented, and final holds accepted submissions.
- Bucket smoke proof on 2026-07-08 accepted one normalized submission and rejected the duplicate from final submission fingerprints in `buckets/smoke-bucket/`.
- Chainstormer 100-loop game-idea run completed on 2026-07-08, but collapsed to 7 unique outputs because the input recursively nested prior chain state; next pass needs compact state, novelty checks, or bucket-backed duplicate rejection.
- Chainstormer now uses compact tangent prompts: `IDEA TYPE`, `THINK TANGENTIALLY ABOUT`, and `RULE`, with default reply length `one line`.
- Chainstormer 100-loop tangent game-idea run on 2026-07-08 produced 92 unique outputs, improving over the prior 7 unique outputs, but later turns still drifted away from game ideas into generic vertical-farming concepts.
- Chainstormer now uses two provider calls per loop: tangent `thought_output`, then detached translation into `idea_output`; 100 loops should complete 200 model calls.
- Chainstormer 100-loop two-call run on 2026-07-08 completed 200/200 calls and improved coherent idea uniqueness to 98, but late turns still drifted into non-game app ideas.
- DSK domain translator workflow lives in `workflow_harnesses/dsk_domain_translator/`; `--subdomains` controls breadth and `--depth` controls recursive layers. Root costs 1 model call; each non-root child costs 2 calls.
- DSK domain translator depth-2 smoke on 2026-07-08 completed 13/13 calls for 7 unique domain nodes, but some generated domain names were weak, so future quality gates should score domain specificity.
- DSK translator breadth comparison on 2026-07-08 showed current subdomain generation is weak: breadth 3 repeated the root as suffixed duplicates, while breadth 10 produced repeated living-toolkit variants plus slot-style filler. It needs stronger sibling diversity prompts and quality gates before large runs.
- DSK translator run IDs now include milliseconds and process id because parallel runs previously collided when started in the same second.
- Prompt-only DSK tuning reduced slot/slice filler and root-copy issues in breadth 10, but the model drifted when prompt wording used "kebab-case"; replacing that with "dash-separated technical name" fixed the food-domain drift while root-like children still remain.
- DSK translator now has reject/repair/fallback acceptance: generated records are rejected for duplicate, parent-copy, filler, drift, repeated-purpose, or missing contract fields; after repair attempts fail, a deterministic axis fallback preserves uniqueness and requested count.
- Final 50-subdomain validation on 2026-07-08 produced 51 records, 51 unique domains, 0 duplicate domains, 0 validation-error records, and 0 weak banned names at `runs/workflow-harnesses/dsk-domain-translator/20260708-070900-675-10495/domain-records.json`.
- Mario-like platformer run with `--subdomains 5 --depth 4` completed on 2026-07-08 with 781 unique records and no validation errors, but all records were fallback-generated, so semantic variety is axis-template-like rather than model-discovered.
- DSK translator was switched to `10.0.0.38` with `lfm2.5-350m-heretic-high-reasoning`; latest matrix-backed Mario 50-subdomain run produced 51 records, 51 unique domains, 50 unique child surface/axis pairs, 0 numeric suffix domains, and 0 validation errors.
- Domain discovery workflow lives in `workflow_harnesses/domain_discovery/`; it loops over a game source name/description, passes accepted domains back as forbidden memory, and accepts at most three new comma-separated dash-name domains per turn.
- Domain discovery defaults to `10.0.0.137` with `lfm2.5-1.2b-instruct`; benchmark proof on 2026-07-08 produced 43 accepted domains across 4 games and 20 loops at `runs/workflow-harnesses/domain-discovery/benchmarks/20260708-133212-333/benchmark-report.json`.
- Lesson: plain "find more domains" prompts caused repeats and app/plugin labels; rotating discovery lanes and required source-phrase hints made the 5-loop benchmark pass, though semantic near-duplicates still need a stronger clustering gate later.
- Subdomain discovery workflow lives in `workflow_harnesses/subdomain_discovery/`; it queues each seed domain and child subdomain, uses a full JSON ledger for duplicate/state tracking, and injects compact recent ledger memory into each prompt.
- Subdomain discovery smoke on 2026-07-08 passed on `10.0.0.137` `lfm2.5-1.2b-instruct` with 3 root domains, 6 turns, 7 accepted subdomains, and 2 exhausted turns at `runs/workflow-harnesses/subdomain-discovery/20260708-133735-394/report.md`.
- Category discovery workflow lives in `workflow_harnesses/category_discovery/`; it discovers a flat list of categories associated with an input before forcing domain hierarchy.
- Category discovery smoke on 2026-07-08 passed on `10.0.0.137` `lfm2.5-1.2b-instruct` with 23 categories over 6 turns at `runs/workflow-harnesses/category-discovery/20260708-205117-272/report.md`.
- Lesson: category discovery gives better raw material than direct hierarchy, but still needs a later merge/grouping stage for near-duplicates such as collection/collecting variants.
- Sharded JSONL ingestion service lives in `kituniverse_harness/ingestion.py`; it creates `manifest.json`, shard-owned JSONL files, and validation reports for high-concurrency dataset writes.
- Ingestion stress proof on 2026-07-08 wrote 256 records at concurrency 256 across 256 shards with 0 malformed records and 0 failures at `runs/ingestion/20260708-221709-484/stress-report.json`.
- Ingestion rule: use sharded JSONL as the live write target for high-concurrency matrix runs, then derive SQLite/DuckDB later for analysis if needed.
- MatrixNodeExplorer-Harness lives in `workflow_harnesses/matrix_node_explorer/`; each async matrix instance runs chained generation steps, asks per-candidate semantic Y/N game checks, and writes a full record through sharded JSONL ingestion.
- Smart routing service lives in `kituniverse_harness/smart_router.py`; it enforces `max_context_tokens` and `max_predictions` so matrix instance concurrency can be higher than active provider calls.
- MatrixNodeExplorer routed proof on 2026-07-08 completed `--nodes 16 --concurrency 256 --max-predictions 128 --max-context-tokens 100 --min-chain-steps 4 --max-chain-steps 8 --list-goal "Game Ideas"` with 16 JSONL records and 0 malformed ingestion records at `runs/workflow-harnesses/matrix-node-explorer/20260708-223053-928/report.json`.
- Lesson: the heretic 350m model often ignores commas or emits artifacts under high temperature; MatrixNodeExplorer now preserves artifacts and uses convert-to-goal plus later filtering rather than over-rejecting during exploration.
- First-stage MatrixNodeExplorer policy: maximize breadth while only rejecting obvious waste such as empty, numeric-only, pure-formatting junk, and exact duplicate outputs; near-duplicates, rough labels, weak grammar, and odd connected ideas should pass to later filtering.
- MatrixNodeExplorer first-stage prompt/report metadata now matches the aggressive expansion contract: keep non-empty, exact-unique, record-safe, loosely relevant outputs and defer awkward wording, weak grammar, near-duplicates, rough labels, and odd connected ideas to later filter/merge/reduce/map stages.
- MatrixNodeExplorer now preserves rough first-stage candidates when `CONVERT TO GOAL` collapses into generic filler such as `one`, `rough`, `name`, or `blue`.
- Latest first-stage policy proof on 2026-07-08: `runs/workflow-harnesses/matrix-node-explorer/20260708-225430-683/report.json` completed 6 nodes, accepted 38 broad outputs, kept router caps at 100 context tokens and 128 predictions, and wrote 6 sharded JSONL records with 0 malformed records.
- FractalKitPipeline lives in `workflow_harnesses/fractal_kit_pipeline/` and is split into the orchestrator, kit contract module, simulator slot smoke, and Playwright slot smoke.
- FractalKitPipeline has a separate stage contract map in `workflow_harnesses/fractal_kit_pipeline/stage_contracts.py`; setup writes `stage-contracts.json`, and `manifest.json` uses the same ordered `stage_graph`.
- FractalKitPipeline model prompts avoid naming the final Nexus kit target; they ask for expansion points, revealed reusable information, and reduced capability names, then deterministic mapping creates Nexus-style kit records.
- FractalKitPipeline first-stage expansion now uses the broad policy: expand aggressively, reject only obvious waste, keep rough or odd candidates if they pass a permissive Y/N relevance check, and defer semantic cleanup to filter/merge/reduce/map stages.
- FractalKitPipeline first-stage prompt stance is fixed as `RULE: Generate unusual but relevant new items`, `AVOID: exact repeats only`, `ALLOW: rough names, strange angles, partial concepts`, `DO NOT POLISH: preserve awkward, partial, or weird connected ideas`, and `RETURN: comma-separated list`; first-stage acceptance should only enforce relevance, exact uniqueness, and record safety.
- FractalKitPipeline first-stage parsing treats comma/newline output as multiple candidates but preserves a single rough phrase as one candidate; do not split phrase output into individual words because that creates parser artifacts too early.
- FractalKitPipeline has a separate setup stage in `workflow_harnesses/fractal_kit_pipeline/setup_stage.py`; it owns control validation, run id creation, run directory creation, final bucket creation, provider health checks, manifest writes, and research-pack writes.
- FractalKitPipeline has a separate temperature schedule service in `workflow_harnesses/fractal_kit_pipeline/temperature_schedule.py`; it owns the high-to-low model temperature policy used by expansion, relevance checks, reveal/reduce, recursive merge review, and slot decision stages.
- FractalKitPipeline has a separate expansion stage in `workflow_harnesses/fractal_kit_pipeline/expansion_stage.py`; it owns broad first-stage generation, obvious-waste rejection, parsing, fallback, and permissive Y/N relevance checks.
- FractalKitPipeline has a separate reveal/reduce stage in `workflow_harnesses/fractal_kit_pipeline/reveal_reduce_stage.py`; it owns expansion-point to revealed-information to reduced-capability processing with high-to-lower temperature scaling.
- FractalKitPipeline has a separate idea matrix stage in `workflow_harnesses/fractal_kit_pipeline/idea_matrix_stage.py`; it owns deterministic matrix expansion from reduced capabilities into kit-shaped candidate records.
- FractalKitPipeline has a separate atomic filter stage in `workflow_harnesses/fractal_kit_pipeline/atomic_filter_stage.py`; it owns atomic/idempotent filtering and duplicate semantic-key rejection.
- FractalKitPipeline writes `expansion-report.json` with first-stage policy metadata, accepted/rejected counts, and rejection samples.
- FractalKitPipeline has a separate domain merge stage in `workflow_harnesses/fractal_kit_pipeline/domain_merge_review.py`; it recursively reviews similar domain paths through low-temperature Y/N gates and preserves aliases, source record ids, needs, and dependencies.
- FractalKitPipeline has a separate domain merge input audit stage in `workflow_harnesses/fractal_kit_pipeline/domain_merge_input_audit_stage.py`; it writes `domain-merge-input-audit.json` and blocks completion if filtered broad candidates are not target-sized, namespaced, broad enough across parent domains/categories, aligned to the domain merge report, and cleanly Y/N reviewed.
- FractalKitPipeline has a separate domain canonicalization stage in `workflow_harnesses/fractal_kit_pipeline/domain_canonicalization_stage.py`; it applies reviewed canonical domain groups back onto every candidate record before kit-level merge review and writes `domain-canonicalized.jsonl`.
- FractalKitPipeline has a separate domain canonicalization audit stage in `workflow_harnesses/fractal_kit_pipeline/domain_canonicalization_audit_stage.py`; it writes `domain-canonicalization-audit.json` and blocks completion if canonicalized records lose filtered record ids, source payload fields, aliases, canonical domain metadata, or merge evidence source ids.
- FractalKitPipeline has a separate kit merge stage in `workflow_harnesses/fractal_kit_pipeline/kit_merge_review.py`; it recursively reviews kit records through need/dependency Y/N gates and owns final record selection.
- FractalKitPipeline has a separate kit canonicalization stage in `workflow_harnesses/fractal_kit_pipeline/kit_canonicalization_stage.py`; it applies reviewed same-kit groups back onto every candidate record before final selection and writes `kit-canonicalized.jsonl`.
- FractalKitPipeline has a separate final quality gate in `workflow_harnesses/fractal_kit_pipeline/final_quality_gate_stage.py`; it verifies target count, uniqueness, JSON safety, required slots, atomic/idempotent flags, renderer boundary, and domain plus kit canonicalization metadata before bucket ingestion.
- FractalKitPipeline has a separate diversity audit in `workflow_harnesses/fractal_kit_pipeline/diversity_audit_stage.py`; it verifies final kit breadth across canonical domains, categories, names, canonical kit ids, merge keys, semantic keys, and build-relevant signatures.
- FractalKitPipeline has a separate dependency graph audit in `workflow_harnesses/fractal_kit_pipeline/dependency_graph_audit_stage.py`; it verifies final `requires`/`provides` graph coherence, namespaced dependency tokens, primary dependency coverage, and no direct require/provide self-edges.
- FractalKitPipeline has a separate build batch manifest stage in `workflow_harnesses/fractal_kit_pipeline/build_batch_manifest_stage.py`; it writes `build-batches.json` and assigns every final kit exactly once into bounded primary-dependency build batches.
- FractalKitPipeline has a separate build batch replay smoke in `workflow_harnesses/fractal_kit_pipeline/build_batch_replay_smoke_stage.py`; it writes `build-batch-replay-smoke.json` and proves every bounded batch can be reconstructed from final records with required slots present.
- FractalKitPipeline has a separate build work order stage in `workflow_harnesses/fractal_kit_pipeline/build_work_order_stage.py`; it writes `build-work-orders.jsonl`, one bounded downstream work order per build batch with source paths, record ids, controls, expected artifacts, and resume hints.
- FractalKitPipeline has a separate stage ledger in `workflow_harnesses/fractal_kit_pipeline/stage_ledger_stage.py`; it maps every stage contract to live artifact paths, counts, gate status, and resume hints.
- FractalKitPipeline has a separate objective audit stage in `workflow_harnesses/fractal_kit_pipeline/objective_audit_stage.py`; it writes `objective-audit.json` and marks `completion_ready` only when a run proves the durable 10k staged harness objective.
- FractalKitPipeline has a separate run artifact helper in `workflow_harnesses/fractal_kit_pipeline/run_artifacts.py`; it owns run ids, research-pack metadata, JSON/JSONL writes, JSONL validation counts, error reports, and markdown report output.
- FractalKitPipeline has a separate final output stage in `workflow_harnesses/fractal_kit_pipeline/final_output_stage.py`; it owns final stage artifact writes, `final-kits.jsonl`, final sharded bucket ingestion, JSONL validation counts, stage ledger writes, objective audit writes, failed-ingest capture, and report assembly.
- FractalKitPipeline 10k proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-230141-484/report.json` produced `final-kits.jsonl` with 10,000 records, 10,000 unique ids, 10,000 semantic keys, 121 domains, 24 families, 0 malformed records, 256 LM merge-review pairs, 640 routed model calls, max context 100, and max predictions 128.
- Final bucket proof on 2026-07-08: `runs/final-buckets/fractal-kit-pipeline/20260708-230141-484/` stored the same 10,000 records across 256 shards with 0 malformed records.
- Simplified NexusSimulator slot smoke passed inside the 10k run, testing 64 kit records through reset, step loop, snapshot, and report.
- Playwright slot smoke passed against the 10k JSONL with 64/64 accepted records, all network routes aborted, and no artifact writes.
- FractalKitPipeline first-stage policy smoke on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-231228-897/report.json` produced 64 final records, 0 malformed JSONL, 0 duplicate ids, accepted 13/19 first-stage candidates, rejected 6, and kept router caps at 100 context tokens and 128 predictions.
- Playwright replay for the first-stage policy smoke accepted 16/16 final records, replayed 4/4 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline domain-merge 10k proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-231532-860/report.json` produced 10,000 final records, 10,000 unique ids, 10,000 semantic keys, 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, 121 domain paths, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,678 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the domain-merge 10k proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline kit-merge module proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-231940-336/report.json` produced 10,000 final records after extracting kit merge review, with 10,000 unique ids, 10,000 semantic keys, 0 malformed JSONL, 0 duplicate ids, 121 domain paths, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,624 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the kit-merge module proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline expansion/reveal module proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-232541-480/report.json` produced 10,000 final records after extracting expansion and reveal/reduce stages, with 10,000 unique ids, 10,000 semantic keys, 0 malformed JSONL, 0 duplicate ids, 216/348 first-stage candidates accepted, 216 reveal/reduce records, 121 domain paths, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,682 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the expansion/reveal module proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline matrix/filter module proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-233026-014/report.json` produced 10,000 final records after extracting idea matrix and atomic filter stages, with 10,000 unique ids, 10,000 semantic keys, 0 malformed JSONL, 0 duplicate ids, 210/344 first-stage candidates accepted, 210 reveal/reduce records, 11,000 matrix candidates, 11,000 atomic-filter kept records, 121 domain paths, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,663 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the matrix/filter module proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline artifact module proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-233626-084/report.json` produced 10,000 final records after extracting run artifact helpers, with manifest/report generation, 10,000 unique ids, 10,000 semantic keys, 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, 206/339 first-stage candidates accepted, 206 reveal/reduce records, 11,000 matrix candidates, 11,000 atomic-filter kept records, 121 domain paths, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,649 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the artifact module proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline first-stage prompt contract proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-234118-275/report.json` completed 64 records with 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, and recorded the broad prompt stance plus later `filter -> merge -> reduce -> map` cleanup order in `expansion-report.json`.
- Playwright replay for the first-stage prompt contract proof accepted 64/64 final records, replayed 4/4 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline final-output stage proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-234420-753/report.json` produced 10,000 final records after extracting final output handling, with 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 201/337 first-stage candidates accepted, 201 reveal/reduce records, 11,000 atomic-filter kept records, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,634 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the final-output stage proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline setup stage proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-234954-823/report.json` produced 10,000 final records after extracting setup handling, with provider health, manifest, research pack, and final bucket setup intact; 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 190/335 first-stage candidates accepted, 190 reveal/reduce records, 11,000 atomic-filter kept records, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,612 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the setup stage proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline temperature schedule proof on 2026-07-08: `runs/workflow-harnesses/fractal-kit-pipeline/20260708-235520-290/report.json` produced 10,000 final records after centralizing high-to-low temperatures, with manifest schedule `{expand: 1.4, reveal: 0.9, reduce: 0.45, relevance_check/domain_merge_review/kit_merge_review/slot_decision: 0.1}`, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 197/326 first-stage candidates accepted, 197 reveal/reduce records, 11,000 atomic-filter kept records, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,617 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the temperature schedule proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline domain canonicalization proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-000050-085/report.json` produced 10,000 final records after applying reviewed domain merge groups to 11,000/11,000 filtered records before kit merge, wrote `domain-canonicalized.jsonl`, preserved aliases/needs/dependencies/source ids, kept 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 1,653 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the domain canonicalization proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline kit canonicalization proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-000643-578/report.json` produced 10,000 final records after applying reviewed same-kit groups to 11,000/11,000 filtered records before final selection, wrote `kit-canonicalized.jsonl`, preserved alias record ids/names/needs/dependencies/review evidence, kept 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 47 canonical domain groups, 74 domain merge reviews, 256 kit merge reviews, 256 reviewed same-kit pairs, 10,744 canonical kit groups, 1,660 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the kit canonicalization proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline final quality gate proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-001231-407/report.json` kept 10,000/10,000 final records with 0 rejections, 10,000 unique record ids, 10,000 unique semantic keys, 10,000 unique merge keys, 47 canonical domains, 10,000 canonical kit ids in the final set, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 1,639 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the final quality gate proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline stage contracts proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-001752-957/report.json` wrote `stage-contracts.json` with 14 stage contracts, matched `manifest.json` `stage_graph` to those stage names exactly, and produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 0 malformed bucket records, 0 failed ingests, 10,000/10,000 records passing final quality, 1,622 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the stage contracts proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline first-stage parser refinement proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-002439-140/report.json` preserved single rough phrase outputs as one candidate while still expanding comma/newline lists, produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, 0 failed ingests, accepted 202/334 first-stage candidates, rejected 123 loose relevance failures and 9 exact duplicates, used 1,635 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the first-stage parser refinement proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline stage ledger proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-003019-091/report.json` wrote `stage-ledger.json` with 14/14 stages matching the stage contracts, no missing contracts, no failed gates, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, 0 failed ingests, 1,653 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the stage ledger proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline objective audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-003535-524/report.json` wrote `objective-audit.json` with `ok=true`, `completion_ready=true`, 13/13 checks passed, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 malformed bucket records, 0 failed ingests, stage ledger ok, 1,639 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the objective audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline diversity audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-004311-503/report.json` wrote `diversity-audit.json` with 47 canonical domains, 24 categories, 10,000 unique build signatures, max duplicate signature count 1, stage ledger 15/15, objective audit `completion_ready=true`, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 failed ingests, 1,634 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the diversity audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline dependency graph audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-004841-623/report.json` wrote `dependency-graph-audit.json` with 8 required tokens, 1,753 provided tokens, 8 primary dependency roots, 0 direct self edges, 0 malformed tokens, stage ledger 16/16, objective audit `completion_ready=true`, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 failed ingests, 1,652 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the dependency graph audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline build batch manifest proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-005404-411/report.json` wrote `build-batches.json` with 83 batches across 8 primary dependency groups, max batch size 128, 0 duplicate assignments, 0 missing assignments, stage ledger 17/17, objective audit `completion_ready=true`, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 failed ingests, 1,640 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build batch manifest proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline build batch replay smoke proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-005935-480/report.json` wrote `build-batch-replay-smoke.json` with 83/83 batches replayed, 10,000 records replayed, 0 failed batches, stage ledger 18/18, objective audit `completion_ready=true`, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 failed ingests, 1,610 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build batch replay smoke proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline build work order proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-010507-626/report.json` wrote `build-work-orders.jsonl` with 83 work orders for 83 batches and 10,000 assigned records, stage ledger 19/19, objective audit `completion_ready=true`, and produced 10,000 final records, 10,000 JSONL lines, 10,000 final bucket records, 0 malformed JSONL, 0 duplicate ids, 0 failed ingests, 1,629 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build work order proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline first-stage aggressive expansion proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-010955-166/report.json` completed a 32-record bounded run with `expansion-report.json` recording `DO NOT POLISH`, explicit `reject_only`, `keep_if`, `do_not_reject_for`, and later `filter -> merge -> reduce -> map`; it accepted 21/21 first-stage candidates, produced 32 final records, 32 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 1 build work order, stage ledger 19/19, and Playwright replay accepted 16/16 records with 4/4 decision traces replayed.
- FractalKitPipeline has a separate build batch packet stage in `workflow_harnesses/fractal_kit_pipeline/build_batch_packet_stage.py`; it writes `build-inputs/<batch-id>/kit-records.jsonl`, `work-order.json`, and `packet-report.json` for every build work order so downstream workers can load one bounded packet instead of the whole 10k JSONL.
- FractalKitPipeline build batch packet proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-011438-271/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 83 build batches, 83 work orders, 83 materialized build input packets, 10,000 packet records, 0 missing packet record refs, 0 malformed packet JSONL files, stage ledger 20/20, objective audit `completion_ready=true`, 1,868 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build batch packet proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate build batch dry-run stage in `workflow_harnesses/fractal_kit_pipeline/build_batch_dry_run_stage.py`; it consumes every materialized build packet independently and writes `batch-results/<batch-id>/build-report.json` plus `build-batch-dry-run-report.json`.
- FractalKitPipeline build batch dry-run proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-012003-453/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 83 build batches, 83 work orders, 83 build input packets, 83 batch dry-run reports, 10,000 records checked, 0 failed batches, stage ledger 21/21, objective audit `completion_ready=true`, 1,819 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build batch dry-run proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate build promotion index stage in `workflow_harnesses/fractal_kit_pipeline/build_promotion_index_stage.py`; it writes `build-promotion-index.json`, a ready/blocked queue over dry-run-passing build batches with source packet paths, build report paths, record counts, and next expected promotion artifacts.
- FractalKitPipeline build promotion index proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-012450-224/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 83 build batches, 83 work orders, 83 build input packets, 83 dry-run batch reports, 83 promotion-ready batches, 0 blocked batches, 10,000 promotion-index records, stage ledger 22/22, objective audit `completion_ready=true`, 1,842 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the build promotion index proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate promoted batches stage in `workflow_harnesses/fractal_kit_pipeline/promoted_batch_stage.py`; it consumes `build-promotion-index.json` and writes `promoted-batches/<batch-id>/promotion-report.json` plus `promoted-batches-report.json`.
- FractalKitPipeline promoted batches proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-013006-221/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 83 build batches, 83 work orders, 83 build input packets, 83 dry-run batch reports, 83 promotion-ready batches, 83 promoted batch reports, 0 blocked promotions, 10,000 promoted records, stage ledger 23/23, objective audit `completion_ready=true`, 1,828 routed LM calls, max context 100, and max predictions 128.
- Playwright replay for the promoted batches proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate handoff manifest stage in `workflow_harnesses/fractal_kit_pipeline/handoff_manifest_stage.py`; it writes `handoff-manifest.json` as the compact start-here map for future agents and downstream builders.
- FractalKitPipeline handoff manifest proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-013705-966/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, 10,000 final bucket records, 83 build batches, 83 work orders, 83 build input packets, 83 promoted batch reports, 10,000 promoted records, `handoff-manifest.json` with 17 artifact pointers, stage ledger 24/24, objective audit `completion_ready=true`, 1,848 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the handoff manifest proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate source shape audit stage in `workflow_harnesses/fractal_kit_pipeline/source_shape_audit_stage.py`; it writes `source-shape-audit.json` and verifies every final record against the source-reviewed ProtoKit/domain-kit shape in `research-pack.json`.
- FractalKitPipeline source shape audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-014459-403/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, checked 10,000/10,000 source-shaped records with 0 failures, 83 build batches, 83 build input packets, 83 promoted batch reports, `handoff-manifest.json` with 18 artifact pointers, stage ledger 25/25, objective audit `completion_ready=true`, 1,833 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the source shape audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a separate final bucket reconciliation stage in `workflow_harnesses/fractal_kit_pipeline/final_bucket_reconciliation_stage.py`; it writes `final-bucket-reconciliation.json` and compares exact record ids between `final-kits.jsonl` and the sharded final bucket.
- FractalKitPipeline final bucket reconciliation proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-015036-941/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, matched 10,000 final JSONL ids to 10,000 sharded bucket ids with 0 missing, 0 extra, and 0 bucket duplicates, `handoff-manifest.json` with 19 artifact pointers, stage ledger 26/26, objective audit `completion_ready=true`, 1,841 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the final bucket reconciliation proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline now persists deterministic matrix candidates in `idea-matrix.jsonl` before atomic/idempotent filtering, so the chain has a durable feed-forward artifact boundary before filtering.
- FractalKitPipeline persisted idea matrix proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-015539-836/report.json` wrote `idea-matrix.jsonl` with 11,000 records, matched `filter-report.json` raw count 11,000, kept 11,000 atomic/idempotent records, produced 10,000 final records, kept final JSONL and final bucket reconciliation clean, `handoff-manifest.json` with 20 artifact pointers, stage ledger 26/26, objective audit `completion_ready=true`, 1,817 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the persisted idea matrix proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline now persists filtered atomic/idempotent candidates in `filtered-candidates.jsonl` before recursive domain merge review.
- FractalKitPipeline persisted filtered candidates proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-020045-608/report.json` wrote `filtered-candidates.jsonl` with 11,000 records, matched `filter-report.json` kept count 11,000 with 0 rejections, produced 10,000 final records, kept final JSONL and final bucket reconciliation clean, `handoff-manifest.json` with 21 artifact pointers, stage ledger 26/26, objective audit `completion_ready=true`, 1,864 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the persisted filtered candidates proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline now persists selected final records in `selected-final-records.jsonl` before final quality and downstream validation stages.
- FractalKitPipeline persisted final selection proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-020529-743/report.json` wrote `selected-final-records.jsonl` with 10,000 records, final quality kept 10,000/10,000 with 0 rejections, produced clean final JSONL and final bucket reconciliation, `handoff-manifest.json` with 22 artifact pointers, stage ledger 26/26, objective audit `completion_ready=true`, 1,857 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the persisted final selection proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a feed-forward artifact integrity stage in `workflow_harnesses/fractal_kit_pipeline/feed_forward_artifact_integrity_stage.py`; it verifies every persisted JSONL stage artifact exists, parses, and matches expected counts.
- FractalKitPipeline feed-forward artifact integrity proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-021052-193/report.json` verified 8/8 JSONL artifacts with 0 malformed lines and count alignment, including 11,000 matrix/filter/canonicalized records and 10,000 selected/final records; stage ledger 27/27, objective audit `completion_ready=true`, 1,841 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the feed-forward artifact integrity proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a stage contract integrity stage in `workflow_harnesses/fractal_kit_pipeline/stage_contract_integrity_stage.py`; it verifies `manifest.json` `stage_graph`, `stage-contracts.json`, and code-owned stage contracts match before the stage ledger is built.
- FractalKitPipeline stage contract integrity proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-021940-765/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 10,000 final bucket records, verified 28 code contracts against 28 manifest stages and 28 artifact stages, wrote `handoff-manifest.json` with 24 artifact pointers, stage ledger 28/28, objective audit `completion_ready=true`, 1,814 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the stage contract integrity proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a final lineage integrity stage in `workflow_harnesses/fractal_kit_pipeline/final_lineage_integrity_stage.py`; it proves final records descend from persisted feed-forward artifacts and that canonical kit, kit alias, and domain evidence references point to existing upstream records.
- FractalKitPipeline final lineage integrity proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-022637-163/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 10,000 final bucket records, verified 10,000 final records against 10,000 selected records, 11,000 idea/filter/domain/kit canonicalized records, 10,000 canonical kit refs, 10,256 kit alias refs, and 568 domain evidence refs, wrote `handoff-manifest.json` with 25 artifact pointers, stage ledger 29/29, objective audit `completion_ready=true`, 1,872 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the final lineage integrity proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a merge review coverage stage in `workflow_harnesses/fractal_kit_pipeline/merge_review_coverage_stage.py`; it proves recursive domain and kit Y/N merge reviews ran with clean outputs, valid depths, canonicalization count preservation, and final canonical metadata.
- FractalKitPipeline merge review coverage proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-023316-012/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 10,000 final bucket records, verified 74 clean domain Y/N pairs across depths 1 and 2, 256 clean kit Y/N pairs at depth 2, 47 domain canonical groups, 10,744 kit canonical groups, and canonical metadata on all 10,000 final records; stage ledger 30/30, objective audit `completion_ready=true`, 1,862 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the merge review coverage proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a prompt/control indirection stage in `workflow_harnesses/fractal_kit_pipeline/prompt_control_indirection_stage.py`; it audits persisted generation prompts for direct final-target leakage while verifying router controls and high-to-low temperature scheduling.
- FractalKitPipeline prompt/control indirection proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-023939-684/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 10,000 final bucket records, audited 669 persisted generation prompts with no final-target leakage, kept router controls at 100 context tokens and 128 max predictions, wrote `handoff-manifest.json` with 27 artifact pointers, stage ledger 31/31, objective audit `completion_ready=true`, 1,900 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the prompt/control indirection proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a slot decision trace integrity stage in `workflow_harnesses/fractal_kit_pipeline/slot_decision_trace_integrity_stage.py`; it proves sampled LFM slot decision traces cover every simulator-required slot, stay bounded, and agree with the simplified simulator slot smoke.
- FractalKitPipeline slot decision trace integrity proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-024638-241/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 10,000 final bucket records, verified 64 sampled records with 7 required slots each, 448 accepted LFM slot decision nodes, simulator agreement over 64 records, `handoff-manifest.json` with 28 artifact pointers, stage ledger 32/32, objective audit `completion_ready=true`, 1,773 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the slot decision trace integrity proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a downstream build-chain integrity stage in `workflow_harnesses/fractal_kit_pipeline/downstream_build_chain_integrity_stage.py`; it writes `downstream-build-chain-integrity.json` and verifies exact record-id coverage through batches, work orders, packet JSONL files, dry-run results, promotion entries, and promoted reports.
- FractalKitPipeline first-stage record safety now rejects direct target-term leakage before generated candidates can be sent into relevance prompts, preserving the indirect-generation rule.
- FractalKitPipeline downstream build-chain integrity proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-030110-917/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, reconciled 83 build batches, 83 work orders, 83 packets, 83 dry-run batch results, 83 promotion-ready entries, 83 promoted reports, 10,000 packet record ids, and 10,000 promoted records; stage ledger 33/33, objective audit `completion_ready=true`, 1,824 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the downstream build-chain proof accepted 64/64 final records from `runs/workflow-harnesses/fractal-kit-pipeline/20260709-030110-917/final-kits.jsonl`, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline has a stage resume plan stage in `workflow_harnesses/fractal_kit_pipeline/stage_resume_plan_stage.py`; it writes `stage-resume-plan.json` with contract-to-artifact mappings, deferred-final markers, controls, and resume hints for every stage.
- FractalKitPipeline stage resume plan proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-030852-960/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, verified 34/34 resume stage entries with no missing completed artifacts, preserved 100 context tokens, 128 max predictions, and 128 workflow concurrency, and kept objective audit `completion_ready=true`.
- Playwright replay for the stage resume plan proof accepted 64/64 final records from `runs/workflow-harnesses/fractal-kit-pipeline/20260709-030852-960/final-kits.jsonl`, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline first expansion stage now records the exact broad prompt template and acceptance rule in `expansion-report.json`: keep non-empty, exact-unique, record-safe candidates that pass loose `LIST GOAL` relevance; reject only obvious waste; preserve rough or odd connected ideas for later filter/merge/reduce/map.
- FractalKitPipeline now has `first_stage_breadth_audit_stage.py`, which writes `first-stage-breadth-audit.json` and blocks completion if first-stage expansion becomes too narrow or starts semantic cleanup before filter/merge/reduce/map.
- FractalKitPipeline first-stage breadth audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-032657-987/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, accepted 295/311 first-stage candidates, rejected only direct target leakage, exact duplicates, and loose relevance failures, stage ledger 36/36, objective audit `completion_ready=true`, 1,804 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the first-stage breadth audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline reveal/reduce now treats generic reduced outputs such as `reusable`, `reuse`, `reusability`, `module`, and `capability` as filler and falls back to the expansion point before downstream matrix generation.
- FractalKitPipeline now has `revealed_reduced_audit_stage.py`, which writes `revealed-reduced-audit.json` and blocks completion if expansion points do not feed forward into compact non-generic reduced capability phrases.
- FractalKitPipeline revealed/reduced audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-033553-217/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, checked 306 expansion points against 306 reveal/reduce records, kept 240 unique reduced phrases, 0 generic reduced outputs, stage ledger 37/37, objective audit `completion_ready=true`, 1,841 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the revealed/reduced audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline now has `idea_matrix_audit_stage.py`, which writes `idea-matrix-audit.json` and blocks completion if deterministic matrix expansion does not preserve reduced reveal signals, target plus buffer size, required payload fields, semantic uniqueness, and broad domain coverage before filtering.
- FractalKitPipeline idea-matrix audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-034453-100/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, wrote 11,000 matrix records with 11,000 unique semantic keys, 121 domains, 24 categories, 244 reveal signals, stage ledger 38/38, objective audit `completion_ready=true`, 1,831 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the idea-matrix audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline now has `atomic_filter_audit_stage.py`, which writes `atomic-filter-audit.json` and blocks completion if filtered candidates are not a count-accounted, semantic-key unique, atomic/idempotent subset of `idea-matrix.jsonl`.
- FractalKitPipeline atomic filter audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-035202-612/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, verified 11,000 matrix records against 11,000 filtered records, 11,000 unique semantic keys, 0 filter rejections, stage ledger 39/39, objective audit `completion_ready=true`, 1,875 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the atomic filter audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline domain merge input audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-040251-497/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, verified 11,000 filtered records, 121 unique domains, 24 parent domains, 24 categories, 74 reviewed domain pairs, 47 canonical groups, schema index clean with 37 JSON artifacts, stage ledger 40/40, objective audit `completion_ready=true`, 1,836 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the domain merge input audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- FractalKitPipeline domain canonicalization audit proof on 2026-07-09: `runs/workflow-harnesses/fractal-kit-pipeline/20260709-041500-054/report.json` produced 10,000 final records, 10,000 JSONL lines, 0 malformed JSONL, 0 duplicate ids, verified 11,000 filtered records against 11,000 domain-canonicalized records, 11,000 reviewed canonical applications, 47 canonical groups, schema index clean with 38 JSON artifacts, stage ledger 41/41, objective audit `completion_ready=true`, 1,842 routed LM calls, max context 100, max predictions 128.
- Playwright replay for the domain canonicalization audit proof accepted 64/64 final records, replayed 64/64 LFM decision traces, aborted all routes, and wrote no artifacts.
- GuidedKitBuilder lives in `workflow_harnesses/guided_kit_builder/` and uses `description -> structured slot draft -> normalization -> validation/repair -> comparison -> link injection`.
- GuidedKitBuilder keeps source facts authoritative, labels model and deterministic contributions per slot, and permits only one active 350M prediction through the smart router.
- GuidedKitBuilder creates immutable reference-only links from exact `provides -> requires` matches; it does not mutate connected kit contracts.
- GuidedKitBuilder proof `runs/workflow-harnesses/guided-kit-builder/20260709-155152-010/report.json` accepted 10/10 benchmark kits, emitted 7 links, used 10 calls with 0 repairs, wrote 0 malformed records, and measured one peak active prediction.
- KitUniverse 1000 now uses one parameterized batch harness. `--count 1`, `--count 50`, and larger bounded requests share the same source matrix, generation, validation, duplicate, NexusSimulator, Sol review, and journaled promotion loop.
- Each kit remains independently gated and immutable. A passing subset may commit while rejected candidates are quarantined and replaced; failures never advance the requested count.
- Batch generation checkpoints each candidate in `generation-results.jsonl`; resume must reuse completed matrix nodes and can continue a held simulated batch without another LM call.
- Batch duplicate safety compares record IDs, semantic keys, merge keys, and a canonical domain/state/requires/provides signature before promotion.
- The real NexusSimulator owns `kit.contract-proof`; KitUniverse locates its CLI through `--simulator-cli`, `NEXUS_SIMULATOR_CLI`, or `nexus-sim` on `PATH`.
- The first two cumulative records are structural candidates only and both fail the tightened semantic quality gate; promotion-ready progress is 0/1,000 until repaired or regenerated.
- Post-Sol validation must run in a fresh Python process because Sol may edit validator code after the parent process imported it; commit is forbidden when the fresh validator rejects the generated artifact.
- The cumulative ledger distinguishes raw commits from promotion-ready kits and quarantines all invalid records. As of 2026-07-09 19:16, it contains 12 raw records, 8 quarantined records, 4/1,000 promotion-ready kits, and three validated promotion-only links.
- Semantic quality now rejects state/input role overlap, weak implementation steps, placeholder tests, incomplete snapshot coverage, placeholder contract tokens, and known source-correctable misspellings.
- Unguided contracts now infer inputs from the idea text, reject generic `*-kit` domains, derive external requirements from those inputs, and require every output to be exposed through a namespaced provision.
- Unguided one-shot implementation plans are deterministic from accepted state, inputs, outputs, and idempotency. Only explicit intake build plans or deterministic contract plans are promotion-trusted.
- One-shot tests are deterministic contract artifacts covering every input transition, every output contract, duplicate replay, snapshot round-trip, reset defaults, and contract shape; model test lists are not promotion-trusted.
- Owned state rejects bare metadata such as `timestamp`, `entity-id`, and `id`; idempotent kits must explicitly own applied-key state derived from the semantic idempotency key.
- First promotion-ready kit: `guided-kit-damage-cooldown-tracks-invulnerability-windows-03a3cd0c3f`; it passed Sol review, fresh validation, manual semantic review, applied-key ownership, exact snapshots, deterministic plans, and full contract tests.
- Second promotion-ready kit: `guided-kit-health-damage-ledger-28aa9b8a00`; it requires `damage-cooldown:accepted-damage` and creates the first immutable link from damage cooldown to health damage.
- Third promotion-ready kit: `guided-kit-defeat-state-gate-4a699cb3c8`; it requires `health:defeated` and extends the immutable graph to entity defeat/revive events.
- Fourth promotion-ready kit: `guided-kit-respawn-eligibility-gate-f3b80767b3`; it requires `entity:defeated` and extends the graph to respawn eligibility/scheduling.
- The defeat-state raw 350M draft was weak and contributed only the final domain directly. Keep explicit slot provenance and add raw-model semantic contribution reporting without weakening promotion correctness.
- Reports now include `model_semantic_contribution` with model-draft slot count, total semantic slots, and ratio; the respawn gate measured 1/13 or 0.0769.
- Shared boundary suffixes in `accepts ...` clauses must be distributed across every coordinated input phrase; otherwise `scheduled respawn and time tick events` is misread and valid owned state is removed as an input-role overlap.
- Fifth promotion-ready kit: `guided-kit-respawn-schedule-queue-aa9aa0e3a5`; it requires `respawn:scheduled`, provides `respawn:due` and `respawn:cancelled`, and extends the immutable graph to four links.
- The respawn schedule replay passed on one 350M call, one active prediction, Sol medium review, and 29-check fresh validation. Cumulative progress is 13 raw, 8 quarantined, 5/1,000 promotion-ready, and 995 remaining.
- Generator contribution is a promotion requirement, not a reporting-only metric. Normal validation and fresh-process validation both require at least one surviving `model-draft` slot, raising the contract gate to 30 checks and allowing zero-contribution drafts to enter bounded repair before Sol review.
- Prompt leakage values such as literal `DESCRIPTION` and `TITLE` are generic domains. Domain normalization falls back to a title-derived specific domain instead of preserving those values as model contribution.
- Respawn activation attempts `20260709-192517-393` and `20260709-192805-960` were preserved and rejected for zero model contribution and post-Sol generic-domain detection. Run `20260709-193008-548` retained one relevant model purpose, passed 30 checks, and promoted `guided-kit-respawn-activation-request-c68a883fbf`.
- The sixth promotion-ready kit requires `respawn:due`, provides `spawn:requested`, and extends the immutable graph to five links. Cumulative progress is 14 raw, 8 quarantined, 6/1,000 promotion-ready, and 994 remaining.
- Universe turns now persist `connection_quality` with incoming/outgoing counts, exact tokens, peer kit ids, link validation and immutability status, plus unmatched requirements and provisions. Connectivity remains reporting evidence rather than a universal promotion requirement.
- Seventh promotion-ready kit: `guided-kit-spawn-point-reservation-26eb1483f6`; it requires `spawn:requested`, provides `spawn-point:reserved` and `spawn-point:unavailable`, and reports one validated immutable incoming link with no unmatched requirements.
- Spawn-point reservation run `20260709-193416-126` passed on one 350M call, one surviving model purpose, Sol medium review, and 30-check fresh validation. Cumulative progress is 15 raw, 8 quarantined, 7/1,000 promotion-ready, six links, and 993 remaining.
- Committed turns now persist `efficiency` with LM Studio prompt/completion/total tokens, tokens per call and generation second, generation latency, Sol review latency, observed total latency, repair delay, and repair/model call counts. The scope explicitly excludes unavailable structured Sol token usage.
- Spawn placement attempt `20260709-193751-210` was rejected after three identical zero-contribution drafts, two bounded repairs, and 1.5 seconds of repair delay; the required Sol reviewer also returned a usage-limit error until 10:39 PM. No ledger progress changed.
- Zero-contribution repair no longer embeds the failed prior JSON. It regenerates from title/description and requires a six-to-fourteen-word spaced purpose sentence, avoiding deterministic repetition caused by anchoring the 350M model to its bad draft.
- Parameterized batch production proof `runs/workflow-harnesses/kit-universe-batch/20260710-021420-248/final-report.json` promoted exactly 50 kits from 104 attempts across four batches. All promotions passed 31 fresh contract checks, simulator acceptance, Sol medium acceptance, and four duplicate signatures.
- Batch resume proof `runs/workflow-harnesses/kit-universe-batch/20260710-023726-100/final-report.json` resumed after a reviewer timeout with zero LM calls and an unchanged generation ledger hash.
- Current cumulative state is 66 raw records, 58 promotion-ready records, 114 total quarantined candidates, 32 validated reference-only links, and 942 remaining.
