# Intention

Status: active

## Purpose

The current session is setting up NexusEngine-KitUniverse as an agent-oriented harness workspace.

## Notes

- The user wants `agent-it` and `harness-it` applied.
- The desired harness direction is one simple entry point that can invoke sub-harnesses.
- The initial local-model target is `10.0.0.38`, with preference for an LFM high-reasoning model if available.
- Keep responses short and action-oriented.
- Build guided kit contracts from full descriptions while preserving slot provenance, enforcing atomic boundaries, and keeping the 350M workflow to one active model call.
- Grow KitUniverse to 1,000 promotion-ready kits through one parameterized batch loop; keep each kit independently validated, simulated, reviewed, deduplicated, and transactionally promoted.
- Process the external 881,069-record RAWG dataset through a live, epoch-aware, provenance-preserving capability pipeline without repeated operator builds.
- Use the bounded domain act-review loop only for representative or unresolved evidence, preserving every pass and requiring deterministic plus Codex quality gates before downstream semantic merging.
